var actions = yanatra.actions;
var util = yanatra.util;
var env = yanatra.env;

module.exports = {
    yourCustomActionHere( obj ) {
        // check the type of an argument
        if (typeof obj === "object") {
        } else if (typeof obj === "string") {

        } else if (typeof obj === "number") {

        }
    }
};